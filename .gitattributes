# Ensure Gradle wrapper JAR is treated as binary
gradle/wrapper/gradle-wrapper.jar binary

# Ensure shell scripts use LF line endings
*.sh text eol=lf
gradlew text eol=lf

# Ensure Windows batch files use CRLF
*.bat text eol=crlf
gradlew.bat text eol=crlf
